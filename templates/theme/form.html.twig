{% use 'form_div_layout.html.twig' %}

{% block  form_errors %}
    {% if errors|length > 0 %}
        {% for error in errors %}
            <span class="form-auth__field-error">{{ error.message }}</span>
        {% endfor %}
    {% endif %}
{% endblock %}